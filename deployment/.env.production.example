# Environment Configuration for Rapids & Roosts (Production)

# Node Environment
NODE_ENV=production

# Server Port
PORT=3000

# Database Configuration
# SQLite database file for production
DATABASE_URL=file:./production.db

# Session Secret (CHANGE THIS to a strong random secret!)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=CHANGE_THIS_TO_A_SECURE_RANDOM_SECRET

# Gmail API Configuration (for email notifications)
GMAIL_CLIENT_ID=your-production-gmail-client-id
GMAIL_CLIENT_SECRET=your-production-gmail-client-secret
GMAIL_REFRESH_TOKEN=your-production-refresh-token

# Google Sheets Configuration (for booking data)
GOOGLE_SHEET_ID=your-production-google-sheet-id

# Domain (for production deployment)
DOMAIN=https://your-domain.com
